"""
app.pyw - Double-clickable windowed launcher for AI Violence Detection GUI
Runs silently without showing any Command Prompt / Terminal window.
"""

from gui_app import launch

if __name__ == "__main__":
    launch()
